{{ form_start(form) }}
    <div class="w-50">
        {{ form_widget(form) }}
    </div>
    <button class="btn btn-success mt-3">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
