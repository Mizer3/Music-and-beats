{% extends 'base-admin.html.twig' %}

{% block title %}Beats{% endblock %}

{% block body %}
    <h1>Beats</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ beat.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ beat.name }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ beat.prix }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ beat.imageName }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ beat.description }}</td>
            </tr>
            <tr>
                <th>BeatName</th>
                <td>{{ beat.beatName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_beats_index') }}">back to list</a>

    <a href="{{ path('app_admin_beats_edit', {'id': beat.id}) }}">edit</a>

    {{ include('admin_beats/_delete_form.html.twig') }}
{% endblock %}
